(rule
 (alias tailwind)
 (deps
  (:in ./tailwind.css)
  (:config ./tailwind.config.js))
 (targets output.css)
 (action
  (run bunx tailwindcss -c %{config} -i %{in} -o ./output.css))
 (mode
  (promote
   (until-clean)
   (into ./static))))
