(rule
 (alias tailwind)
 (deps
  (:in ./tailwind.css)
  (:config ./tailwind.config.js)
  (glob_files ../lib/components/*.ml))
 (targets output.css)
 (action
  (run npx tailwindcss -c %{config} -i %{in} -o ./output.css))
 (mode
  (promote
   (until-clean)
   (into ../static))))
